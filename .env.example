# Server origin — the console talks to its /api/v1/admin sub-path.
# The /admin API is enabled by setting SUPERADMIN_EMAIL / SUPERADMIN_PASSWORD /
# JWT_ADMIN_SECRET in medipos-server's .env.
VITE_API_URL=http://localhost:3000
